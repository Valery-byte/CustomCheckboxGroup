<Window x:Class="Example.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Example"
        xmlns:tcontrol="clr-namespace:CustomCheckboxGroup;assembly=CustomCheckboxGroup"
        
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Button Grid.Column="1" Content="CheckboxGroup" HorizontalAlignment="Center" Margin="0,0,0,0" VerticalAlignment="Bottom" Click="Button_Click"/>
        <Button Grid.Column="2" Content="CheckboxGroupTopLabel" HorizontalAlignment="Center" Margin="0,0,0,0" VerticalAlignment="Bottom" Click="Button_Click_1"/>
    </Grid>
</Window>
